<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="pl.edu.agh.servicetracker">

    <uses-permission android:name="android.permission.NFC"/>

    <application
            android:allowBackup="true"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
        <activity
                android:name="pl.edu.agh.servicetracker.MainActivity"
                android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name="pl.edu.agh.servicetracker.NFCRequestActivity"
                  android:label="@string/new_request"
                  android:noHistory="true">
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="https"
                      android:host="play.google.com"
                      android:pathPrefix="/store/apps/details?id=com.nxp.nfc.tagwriter"/>
            </intent-filter>
        </activity>

        <activity android:name="pl.edu.agh.servicetracker.NewRequestActivity"
                  android:label="@string/new_request"></activity>

        <activity android:name="pl.edu.agh.servicetracker.RequestListActivity"
                  android:label="@string/my_requests"></activity>

    </application>

</manifest>
